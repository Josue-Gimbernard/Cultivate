# Docs Intake

Place incoming reference docs here.

Use this folder for source material that should remain available for exact lookup. After reviewing a doc, promote durable conclusions into:

- `memory/` for company-wide context, rules, and decisions.
- `domains/` for area status.
- `skills/` for repeatable workflows.

Do not delete original reference docs unless explicitly asked.

